import type { FC } from "react";
import type { BrowserRouterProps } from "react-router-dom";

const Finish: FC<BrowserRouterProps> = () => {
  return (
    <div>
      <h1>This is Finish page.</h1>
    </div>
  );
};

export default Finish;
